name: RedMC-Scoreboard
description: RedMC - Sidebar Scoreboard Customization
version: '0.0.1-alpha'
api-version: '1.21.11'
prefix: RedMC
folia-supported: true
main: red.aviora.redmc.scoreboard.ScoreboardPlugin
bootstrapper: red.aviora.redmc.scoreboard.ScoreboardBootstrap
dependencies:
  server:
    RedMC-API:
      load: BEFORE
      required: true
      join-classpath: true
    RedMC-Placeholders:
      load: BEFORE
      required: true
      join-classpath: true
permissions:
  redmc.scoreboard:
    default: true
  redmc.scoreboard.reload:
    default: op
  redmc.scoreboard.reload.config:
    default: op
  redmc.scoreboard.reload.all:
    default: op
  redmc.scoreboard.toggle:
    default: true
